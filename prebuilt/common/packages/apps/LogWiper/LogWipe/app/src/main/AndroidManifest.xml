<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pwnieexpress.android.logwipe">
    <uses-permission android:name="jackpal.androidterm.permission.RUN_SCRIPT"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/logwipe"
        android:excludeFromRecents="true"
        android:theme="@android:style/Theme.NoDisplay"
        android:label="@string/app_name">
        <activity android:name="com.pwnieexpress.android.logwipe.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>